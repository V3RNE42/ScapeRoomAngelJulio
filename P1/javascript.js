var usuario = document.getElementById("login").value;
var password = document.getElementById("password").value;

console.log(
  "ENCUENTRA A WALLY! HAY 4, SOLO UNO TE DARÁ EL PASO AL SIGUIENTE. SUERTE."
);

function neWindow() {
  if (usuario === "blabibloblu") {
    if (login === "sacolaosacolao") {
      console.log("pantallaCelebrar.html");
    }
  } else {
    console.log("contraseña incorrecta");
  }
}
